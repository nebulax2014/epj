<!DOCTYPE html>

<html class="no-js">
    <head>
        <title></title>

        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <link rel="stylesheet" href="/css/bootstrap.min.css">
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body id="body">
        <div class="heading">
            <p>AutoziFE</p>
        </div>

        <div class="panel panel-default dialog_center">
            <div class="panel-heading">
                <h3 class="panel-title">欢迎登陆</h3>
            </div>

                <div class="form-bottom">
                    <div class="form-group">
                        <label class="sr-only" for="form-name">UserName</label>
                        <input type="text" name="form-login" placeholder="登录名" class="form-control" id="form-name">
                    </div>
                    <div class="form-group">
                        <label class="sr-only" for="form-password">Password</label>
                        <input type="password" name="form-password" placeholder="密码" class="form-control" id="form-password">
                    </div>
                    <a href="javascript:;" class="btn-next">登陆</a>
                    <!--<a href="/register" class="btn-resus">没有账号？点这里</a>-->
                </div>
        </div>
        <script src="/js/jquery-2.1.3.min.js"></script>
        <script src="/js/bootstrap.min.js"></script>
        <script>
            $(function(){

                $('.form-bottom').find('a').eq(0).click(function(){
                    var loginName = $('.form-bottom').find('input[name=form-login]').val();
                    var password = $('.form-bottom').find('input[name=form-password]').val();
                    var data = {"ulogin":loginName,"upassword":password};
                        $.ajax({
                            url:'/login',
                            data:data,
                            type:"post",
                            success:function(status){
//                                alert(status.state);
                                window.location.href ="/admin";
                            }
                        });
                });
            });
        </script>
    </body>
</html>

